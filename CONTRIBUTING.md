# Contributing

Profile changes must remain factual, dated, and linked to reproducible evidence.
Do not promote a project to preview or stable based on documentation alone.

Run:

```bash
node scripts/check-profile.mjs --root .
node --test test/*.test.mjs
```

Use synthetic fixtures only and disclose material AI assistance.

